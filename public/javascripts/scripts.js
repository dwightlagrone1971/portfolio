function move() {
    var elem = document.getElementById("myBar");
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 30) {
        clearInterval(id);
      } else {
         width++; 
         elem.style.width = width + '%'; 
      }
   }
}

function move2() {
    var elem2 = document.getElementById("myBar2"); 
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 50) {
        clearInterval(id);
      } else {
         width++; 
         elem2.style.width = width + '%'; 
      }
   }
}

function move3() {
    var elem2 = document.getElementById("myBar3"); 
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 40) {
        clearInterval(id);
      } else {
         width++; 
         elem2.style.width = width + '%'; 
      }
   }
}

function move4() {
    var elem2 = document.getElementById("myBar4"); 
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 50) {
        clearInterval(id);
      } else {
         width++; 
         elem2.style.width = width + '%'; 
      }
   }
}

function move5() {
    var elem2 = document.getElementById("myBar5"); 
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 30) {
        clearInterval(id);
      } else {
         width++; 
         elem2.style.width = width + '%'; 
      }
   }
}

function move6() {
    var elem2 = document.getElementById("myBar6"); 
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 90) {
        clearInterval(id);
      } else {
         width++; 
         elem2.style.width = width + '%'; 
      }
   }
}
function move7() {
    var elem2 = document.getElementById("myBar7"); 
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 85) {
        clearInterval(id);
      } else {
         width++; 
         elem2.style.width = width + '%'; 
      }
   }
}
function move8() {
    var elem2 = document.getElementById("myBar8"); 
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 90) {
        clearInterval(id);
      } else {
         width++; 
         elem2.style.width = width + '%'; 
      }
   }
}
function move9() {
    var elem2 = document.getElementById("myBar9"); 
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 50) {
        clearInterval(id);
      } else {
         width++; 
         elem2.style.width = width + '%'; 
      }
   }
}
function move10() {
    var elem2 = document.getElementById("myBar10"); 
    var width = 1;
    var id = setInterval(frame, 10);
    function frame() {
      if (width >= 40) {
        clearInterval(id);
      } else {
         width++; 
         elem2.style.width = width + '%'; 
      }
   }
}